<UserControl x:Class="Manager_proj_4.UserControls.Sqlite_StatusTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Manager_proj_4.UserControls"
			 
			 xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
			 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
	<Grid>
		<Grid Margin="0,0,0,35">
			<DataGrid x:Name="dataGrid" IsReadOnly="True">
				<DataGrid.Style>
					<Style BasedOn="{StaticResource MetroDataGrid}" TargetType="{x:Type DataGrid}">
						<Setter Property="AlternatingRowBackground" Value="{DynamicResource GrayBrush10}" />
					</Style>
				</DataGrid.Style>
			</DataGrid>
		</Grid>
		<Grid Height="30" VerticalAlignment="Bottom">
			<Button Content="KillAll"
					Margin="0,0,230,0" Width="100" Height="30" HorizontalAlignment="Right"/>
			<Button Content="Kill"
					Margin="0,0,120,0" Width="100" Height="30" HorizontalAlignment="Right"/>
			<Button Content="Cancel"
					Margin="0,0,10,0" Width="100" Height="30" HorizontalAlignment="Right"/>
		</Grid>
	</Grid>
</UserControl>
